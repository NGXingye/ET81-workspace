> 最新修改时间：2026-09-21 18:15 UTC+8
> 版本号：1.3.0
> 文档状态：生效
> 读取等级：L1（活卡占用时停放分析包前读取）

# 0. 职责定位

本目录停放**未开工**的分析包，最多 2 个 JSON。不是第二张活契约，不能 `/implement`，即使 `route=fast` 也不能改 trunk。活卡 idle 且用户点名后，由 `etctl promote <task_id>` 写入 `current.md` / `current_scope.json`。

# 1. 文件索引

- [`t015.json`](./t015.json)  
  职责：SkillEditor V2 本地提效管线蓝图（t014 占用活卡时停放）。

分析模型在活卡非 idle 时写入 `<task_id>.json`（复制 `../_template_scope.json`，加上 `goal`；`phase` 仅为 `discovery` 或 `awaiting_scope`）。最多 2 个。

# 2. 版本历史

- 1.3.0（2026-09-21）：登记 t015（技能编辑器本地管线）。
- 1.2.1（2026-09-21）：t013 已 promote，候选清空。
- 1.2.0（2026-09-21）：登记 t013（100280 不放技能）。
- 1.1.1（2026-09-18）：用户取消已解决的 t004。
- 1.1.0（2026-09-18）：登记 t004。
- 1.0.0（2026-09-17）：候选分析包目录；最多 2 个；禁止双 implement。
